import { DaysFormatPipe } from './days-format.pipe';

describe('DaysFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
